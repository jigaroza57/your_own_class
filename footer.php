<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>


</head>

<body>



  <style>
    @media(max-width:768px) {

      .footer-layout2 {
        border-top-left-radius: 0px !important;
        border-top-right-radius: 0px !important;
      }



    }
  </style>

  <!-- marquee Start -->


  <style>
    /* scrolling marquee strip */
    .marquee-strip {
      background: #f38125;
      overflow: hidden;
      white-space: nowrap;
      padding: 6px 0;
    }

    .marquee-track {
      display: inline-block;
      animation: scroll 24s linear infinite;
    }

    .marquee-track span {
      font-size: 20px;
      font-weight: 700;
      color: #fff;
      letter-spacing: 1px;
      padding: 0 30px;
    }

    .marquee-track span::before {
      content: '◆  ';
    }

    @keyframes scroll {
      from {
        transform: translateX(0)
      }

      to {
        transform: translateX(-50%)
      }
    }
  </style>

  <!-- marquee -->
  <div class="marquee-strip">
    <div class="marquee-track">
      <span>સલામતી સાથે શિક્ષણ </span><span>માતા - પિતાની નજર સામે શિક્ષણ </span><span>અડધી કિંમતે શિક્ષણ</span>

      <!-- duplicate for seamless scroll -->
      <span>સલામતી સાથે શિક્ષણ </span><span>માતા - પિતાની નજર સામે શિક્ષણ </span><span>અડધી કિંમતે શિક્ષણ</span>

      <!-- duplicate for seamless scroll -->
      <span>સલામતી સાથે શિક્ષણ </span><span>માતા - પિતાની નજર સામે શિક્ષણ </span><span>અડધી કિંમતે શિક્ષણ</span>

      <!-- duplicate for seamless scroll -->
      <span>સલામતી સાથે શિક્ષણ </span><span>માતા - પિતાની નજર સામે શિક્ષણ </span><span>અડધી કિંમતે શિક્ષણ</span>

      <!-- duplicate for seamless scroll -->
      <span>સલામતી સાથે શિક્ષણ </span><span>માતા - પિતાની નજર સામે શિક્ષણ </span><span>અડધી કિંમતે શિક્ષણ</span>

      <!-- duplicate for seamless scroll -->
      <span>સલામતી સાથે શિક્ષણ </span><span>માતા - પિતાની નજર સામે શિક્ષણ </span><span>અડધી કિંમતે શિક્ષણ</span>

      <!-- duplicate for seamless scroll -->
      <span>સલામતી સાથે શિક્ષણ </span><span>માતા - પિતાની નજર સામે શિક્ષણ </span><span>અડધી કિંમતે શિક્ષણ</span>

    </div>
  </div>

  <!-- marquee End -->


  <footer class="footer-wrapper footer-layout2" style="background: whitesmoke;  border-top: 5px solid #1A3558;">

    <div class="footer-bottom">
      <div class="widget-area ">
        <div class="container">
          <div class="row justify-content-between">
            <div class="col-md-6 col-xl-auto">
              <div class="widget footer-widget th_fade_anim" data-delay=".3">
                <div class="th-widget-about">
                  <div class="about-logo">
                    <a href="index">
                      <img src="img/new_logo.png" style="width: 30vh;" alt="Escul">
                    </a>
                  </div>

                  <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d193420.70436452903!2d72.19606889453127!3d23.003752500000026!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x395e9b2d89af9e37%3A0xe288bf80947aeddf!2sSG%20Highway!5e1!3m2!1sen!2sin!4v1774082661629!5m2!1sen!2sin" width="600" height="200" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
                  <!-- <p class="about-text">Your Own Class ગુજરાત રાજ્યના ખૂણેખૂણે ઉચ્ચ કક્ષાના ઓનલાઇન ક્લાસ, રહેણાંક સ્ટડી સ્કૉલ અને IT-આધારિત કૌશલ્ય તાલીમ પ્રદાન કરે છે. ઉત્પાદન, IT સેવાઓ, કન્સ્ટ્રક્શન, કન્સ્ટ્રક્શ, અને નવીનતાના ક્ષેત્રે પણ અમે સક્રિય છીએ.</p> -->

                </div>
              </div>
            </div>
            <div class="col-sm-6 col-xl-auto">
              <div class="widget widget_nav_menu footer-widget th_fade_anim" data-delay=".5">
                <h3 class="widget_title">Quick Links</h3>
                <div class="menu-all-pages-container">
                  <ul class="menu">


                    <li style="font-size: bold;"><a href="index">Home</a></li>
                    <li style="font-size: bold;"><a href="about">About Us</a></li>

                    <li style="font-size: bold;"><a href="about_new">About YOC</a></li>
                    <li style="font-size: bold;"><a href="parents">Parents</a></li>

                    <li style="font-size: bold;"><a href="teacher">Teachers</a></li>


                  </ul>
                </div>
              </div>
            </div>
            <div class="col-sm-6 col-xl-auto">
              <div class="widget widget_nav_menu footer-widget th_fade_anim mob_top" data-delay=".5">
                <h3 class="widget_title" style="visibility: hidden;">Quick Links</h3>

                <div class="menu-all-pages-container">
                  <ul class="menu">

                    <li><a href="Brand_Ambassador">Brand Ambassador </a></li>


                    <li><a href="Cources">Courses </a></li>

                    <li><a href="contact">Contact Us </a></li>

                  </ul>
                </div>
              </div>
            </div>

            <div class="col-sm-6 col-xl-auto">
              <div class="widget footer-widget th_fade_anim" data-delay=".9">
                <div class="th-widget-contact">
                  <h3 class="widget_title">Get In Touch</h3>

                  <!-- Address -->
                  <div class="info-box">
                    <div class="">
                      <i class="fa-solid fa-location-dot" style="color: black;"></i>
                    </div>
                    <div class="box-details">
                      <label for="" style="font-weight: bold; color: black;">શેઠ સાગા પ્રા. લી. </label>
                      <p class="box-text">
                        C/o, "શેઠ ગ્રુપ", બી-૩૦૨, <br>
                        સપથ હેક્ષા, કારગિલ પેટ્રોલ પંપ સામે, <br>
                        એસ. જી. રોડ, અમદાવાદ.
                      </p>
                    </div>
                  </div>

                  <!-- Phone -->
                  <div class="info-box">
                    <div class="">
                      <i class="fa-solid fa-phone" style="color: black;"></i>
                    </div>
                    <div class="box-details">
                      <p class="box-text">
                        <a href="tel:6356 555 404 " class="box-link">6356 555 404 </a>
                      </p>

                    </div>
                  </div>

                  <!-- Email -->
                  <div class="info-box">
                    <div class="">
                      <i class="fa-solid fa-envelope" style="color: black;"></i>
                    </div>
                    <div class="box-details">
                      <p class="box-text">
                        <a href="mailto:yrownclass@gmail.com" class="box-link">
                          yrownclass@gmail.com
                        </a>
                      </p>
                    </div>
                  </div>

                </div>

              </div>
            </div>
            <!-- <div class="col-sm-6 col-xl-auto">
              <div class="widget footer-widget th_fade_anim" data-delay=".9">
                <div class="th-widget-contact">
                  <h3 class="widget_title">Map</h3>

                  <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d193420.70436452903!2d72.19606889453127!3d23.003752500000026!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x395e9b2d89af9e37%3A0xe288bf80947aeddf!2sSG%20Highway!5e1!3m2!1sen!2sin!4v1774082661629!5m2!1sen!2sin" width="600" height="300" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>

                </div>
              </div>
            </div> -->
          </div>
        </div>
      </div>
      <div class="copyright-wrap">
        <div class="container">
          <div class="row gy-2 align-items-center">
            <div class="col-lg-12 mob_bottom_padding">
              <center>
                <span class="">
                  <p>Copyright &copy; <?php echo date("Y"); ?> The Your Own Class | Designed by <a href="https://apexsoftwarehouse.com/"><b> Apex Software House </b> </a></p>
                </span>
              </center>
            </div>

          </div>
        </div>
      </div>
    </div>
  </footer>




  <style>
    @media (max-width: 768px) {
      .mob_bottom_padding {
        margin-bottom: 45px;
      }

      .mob_top {
        margin-top: -24%;
      }
    }
  </style>

  <style>
    /* Google Translate offset permanent fix */
    body {
      top: 0 !important;
      position: relative !important;
    }

    .goog-te-banner-frame,
    .goog-te-banner-frame.skiptranslate {
      display: none !important;
      visibility: hidden !important;
    }

    #goog-gt-tt,
    .goog-te-balloon-frame {
      display: none !important;
    }
  </style>

  <div class="scroll-top"><svg class="progress-circle svg-content" width="100%" height="100%" viewBox="-1 -1 102 102">
      <path d="M50,1 a49,49 0 0,1 0,98 a49,49 0 0,1 0,-98"
        style="transition: stroke-dashoffset 10ms linear 0s; stroke-dasharray: 307.919, 307.919; stroke-dashoffset: 307.919;">
      </path>
    </svg></div>


  <!-- Desktop + Mobile Button -->
  <a href="#" class="inquiry-btn" onclick="openForm(); return false;">
    <span class="inq-icon"><i class="fa-solid fa-envelope"></i></span>
    <span class="inq-text" style="font-size: 18px;">પૂછપરછ </span>
  </a>

  <!-- Modal -->
  <div id="inquiryModal" class="modal">
    <div class="modal-content">

      <!-- Header Bar -->
      <div class="modal-header">
        <div class="modal-header-left">
          <div class="modal-icon-wrap">✉</div>
          <div>
            <h3>પૂછપરછ ફોર્મ</h3>
            <p class="modal-sub">અમે ટૂંક સમયમાં તમારો સંપર્ક કરીશું</p>
          </div>
        </div>
        <span class="close" onclick="closeForm()">&#x2715;</span>
      </div>

      <!-- Form Body -->
      <div class="modal-body">
        <form class="gform pure-form pure-form-stacked" method="POST" data-email="yrownclass@gmail.com"
          action="https://script.google.com/macros/s/AKfycby6rHZ6Wcu7IujCV4AXbbvFkgs2Hau2hOa4T-cCZZd6RxWNVM1N3lbc0nm1n3IyIm-l/exec" onsubmit="return false;">

          <!-- Role (Full width) -->
          <div class="form-group full">
            <label>Role</label>
            <div class="input-wrap">
              <span class="input-icon">🎓</span>
              <select name="role" required onchange="handleRoleChange(this.value)">

                <option value="">— Select your role —</option>
                <option value="student / Parents">Student / Parents </option>
                <option value="Teacher">Teacher</option>
                <option value="Agent">Agent</option>
                <option value="Classes owner">Classes Owner</option>

              </select>
            </div>
          </div>

          <!-- District combo -->
          <div class="form-group" id="districtGroup">
            <label>District</label>
            <div class="input-wrap combo-wrap" id="districtCombo">
              <span class="input-icon">🏙</span>
              <input type="text" name="district" id="districtInput"
                placeholder="Search or select district..." autocomplete="off" readonly>
              <span class="combo-clear" id="districtClear" onclick="clearDistrict()">✕</span>
              <span class="combo-arrow" id="districtArrow">▼</span>
            </div>
            <div class="ac-dropdown" id="districtDD"></div>
          </div>

          <!-- Taluka combo -->
          <div class="form-group" id="talukaGroup">
            <label>Taluka</label>
            <div class="input-wrap combo-wrap" id="talukaCombo">
              <span class="input-icon">📍</span>
              <input type="text" name="taluka" id="talukaInput"
                placeholder="Search or select taluka..." autocomplete="off" readonly>
              <span class="combo-clear" id="talukaClear" onclick="clearTaluka()">✕</span>
              <span class="combo-arrow" id="talukaArrow">▼</span>
            </div>
            <div class="ac-dropdown" id="talukaDD"></div>
          </div>

          <!-- Full Name -->
          <div class="form-group">
            <label>Full Name</label>
            <div class="input-wrap">
              <span class="input-icon">👤</span>
              <input type="text" name="name" placeholder="Enter your name" required>
            </div>
          </div>

          <!-- Phone -->
          <div class="form-group">
            <label>Phone Number</label>
            <div class="input-wrap">
              <span class="input-icon">📞</span>
              <input type="tel" name="number" maxlength="10" pattern="[0-9]{10}" placeholder="Enter your phone number" required>
            </div>
          </div>



          <!-- Email -->
          <div class="form-group full">
            <label>Email Address</label>
            <div class="input-wrap">
              <span class="input-icon">✉</span>
              <input type="email" name="address" placeholder="Enter your email">
            </div>
          </div>

          <!-- Submit Button -->
          <div class="form-group full">
            <button class="submit-btn">Send Inquiry →</button>
          </div>

          <div class="thankyou_message alert alert-success row col-12" role="alert"
            style="display:none;font-size:16px;padding:15px;border-radius:10px;">
            🎉 Thank you for contacting us! <br>
            ✅ Your message has been received successfully. <br>
            📩 Our team will get back to you shortly.
          </div>

        </form>
      </div>

    </div>
  </div>

  <script>
    /* ── District → Taluka Data ── */
    const DISTRICT_TALUKA = {
      "Ahmedabad": ["Ahmedabad City", "Ahmedabad (Rural)", "Asarva", "Sanand", "Bavla", "Dholka", "Viramgam", "Dhandhuka", "Mandal", "Detroj-Rampura"],
      "Amreli": ["Amreli Rural", "Babra", "Bagasara", "Dhari", "Jafrabad", "Khambha", "Kunkavav-Vadia", "Amreli", "Lathi", "Lilia", "Rajula", "Savar Kundla"],
      "Anand": ["Anand", "Anklav", "Borsad", "Khambhat", "Petlad", "Sojitra", "Tarapur", "Umreth"],
      "Aravalli": ["Bayad", "Bhiloda", "Dhansura", "Malpur", "Meghraj", "Modasa", "Sathamba", "Shamlaji"],
      "Banaskantha": ["Amirgadh", "Dantiwada", "Deesa", "Palanpur", "Palanpur (City)", "Vadgam", "Danta", "Dhanera", "Kankrej", "Lakhani", "Suigam"],
      "Bharuch": ["Amod", "Ankleshwar", "Bharuch", "Hansot", "Jambusar", "Jhagadia", "Netrang", "Vagra", "Valia"],
      "Bhavnagar": ["Bhavnagar", "Gariadhar", "Ghogha", "Mahuva", "Palitana", "Jesar", "Sihor", "Talaja", "Umrala", "Vallabhipur"],
      "Botad": ["Botad", "Barwala", "Gadhada", "Ranpur"],
      "Chhota Udaipur": ["Bodeli", "Chhota Udaipur", "Jetpur Pavi", "Kawant", "Nasvadi", "Sankheda"],
      "Dahod": ["Dahod", "Devgadh Baria", "Dhanpur", "Fatepura", "Garbada", "Govindguru Limdi", "Jhalod", "Limkheda", "Sanjeli", "Santrampur", "Singvad", "Sukhsar"],
      "Dang": ["Ahwa", "Subir", "Waghai"],
      "Devbhoomi Dwarka": ["Bhanvad", "Dwarka", "Kalyanpur", "Khambhalia", "Okhamandal"],
      "Gandhinagar": ["Gandhinagar", "Dehgam", "Kalol", "Mansa"],
      "Gir Somnath": ["Gir Gadhada", "Kodinar", "Patan-Veraval", "Sutrapada", "Veraval", "Talala", "Una"],
      "Jamnagar": ["Dhrol", "Jamnagar", "Jodiya", "Jamjodhpur", "Kalavad", "Lalpur"],
      "Junagadh": ["Junagadh", "Bhesan", "Keshod", "Malia Hatina", "Manavadar", "Mangrol", "Mendarda", "Vanthali", "Visavadar"],
      "Kachchh": ["Abdasa", "Anjar", "Bhachau", "Bhuj", "Gandhidham", "Lakhpat", "Mandvi", "Mundra", "Nakhatrana", "Rapar"],
      "Kheda": ["Kapadvanj", "Kheda", "Mehmedabad", "Kathlal", "Daskroi", "Mahudha", "Matar", "Nadiad", "Thasra", "Vaso"],
      "Mahisagar": ["Balasinor", "Godhra", "Kadana", "Khanpur", "Kothmba", "Lunawada", "Santrampur", "Virpur"],
      "Mehsana": ["Becharaji", "Jotana", "Kadi", "Kheralu", "Mehsana", "Satlasana", "Unjha", "Vadnagar", "Vijapur", "Visnagar"],
      "Morbi": ["Halvad", "Maliya", "Morbi", "Tankara", "Wankaner"],
      "Narmada": ["Dediapada", "Chikhda", "Garudeshwar", "Nandod", "Sagbara", "Tilakwada"],
      "Navsari": ["Chikhli", "Gandevi", "Jalalpore", "Navsari", "Vansda"],
      "Panchmahal": ["Godhra", "Ghoghamba", "Halol", "Jambughoda", "Kalol", "Morwa(Hadaf)", "Shehera"],
      "Patan": ["Chanasma", "Harij", "Patan", "Saraswati", "Radhanpur", "Sami", "Sankheswar", "Santalpur", "Siddhpur"],
      "Porbandar": ["Kutiyana", "Porbandar", "Ranavav"],
      "Rajkot": ["Dhoraji", "Gondal", "Jamkandorna", "Jasdan", "Jetpur", "Kotda Sangani", "Lodhika", "Paddhari", "Rajkot", "Upleta", "Vinchhiya"],
      "Sabarkantha": ["Himatnagar", "Idar", "Khedbrahma", "Vadali", "Poshina", "Prantij", "Talod", "Vijaynagar"],
      "Surat": ["Bardoli", "Ambika", "Adajan", "Choryasi", "Kamrej", "Mandvi", "Mangrol", "Mahuva", "Olpad", "Palsana", "Surat City", "Umarpada"],
      "Surendranagar": ["Chotila", "Dasada", "Dhrangadhra", "Limbdi", "Lakhtar", "Muli", "Sayla", "Surendranagar", "Thangadh", "Wadhwan"],
      "Tapi": ["Dolvan", "Kukarmunda", "Nizar", "Songadh", "Uchchhal", "Ukai", "Valod", "Vyara"],
      "Vadodara": ["Dabhoi", "Desar", "Karjan", "Padra", "Sinor", "Vadodara", "Savli", "Vadodara Rural", "Vadodara City", "Vaghodia", "Waghodia"],
      "Valsad": ["Dharampur", "Kaprada", "Pardi", "Umbergaon", "Valsad"]
    };

    const allDistricts = Object.keys(DISTRICT_TALUKA).sort();
    const allTalukas = [...new Set(Object.values(DISTRICT_TALUKA).flat())].sort();

    /* Reverse map: taluka → [districts] */
    const TALUKA_DISTRICT = {};
    for (const [dist, talukas] of Object.entries(DISTRICT_TALUKA)) {
      talukas.forEach(t => {
        const key = t.toLowerCase();
        if (!TALUKA_DISTRICT[key]) TALUKA_DISTRICT[key] = [];
        if (!TALUKA_DISTRICT[key].includes(dist)) TALUKA_DISTRICT[key].push(dist);
      });
    }

    /* ── Element refs ── */
    const districtInput = document.getElementById("districtInput");
    const talukaInput = document.getElementById("talukaInput");
    const districtDD = document.getElementById("districtDD");
    const talukaDD = document.getElementById("talukaDD");
    const districtArrow = document.getElementById("districtArrow");
    const talukaArrow = document.getElementById("talukaArrow");
    const districtClear = document.getElementById("districtClear");
    const talukaClear = document.getElementById("talukaClear");

    let selectedDistrict = "";
    let selectedTaluka = "";
    let distSearchMode = false;
    let talSearchMode = false;

    /* ── Highlight matched text ── */
    function highlight(text, query) {
      if (!query) return text;
      const idx = text.toLowerCase().indexOf(query.toLowerCase());
      if (idx === -1) return text;
      return text.slice(0, idx) +
        '<mark>' + text.slice(idx, idx + query.length) + '</mark>' +
        text.slice(idx + query.length);
    }

    /* ── Render dropdown items ── */
    function renderDropdown(ul, items, onSelect) {
      ul.innerHTML = "";
      if (!items.length) {
        const li = document.createElement("li");
        li.className = "ac-no-result";
        li.textContent = "No results found";
        ul.appendChild(li);
        ul.style.display = "block";
        return;
      }
      items.forEach(({
        label,
        sub,
        query
      }) => {
        const li = document.createElement("li");
        li.innerHTML = sub ?
          `<span class="ac-main">${highlight(label, query)}</span><span class="ac-sub">${sub}</span>` :
          `<span class="ac-main">${highlight(label, query)}</span>`;
        li.addEventListener("mousedown", e => {
          e.preventDefault();
          onSelect(label, sub);
        });
        ul.appendChild(li);
      });
      ul.style.display = "block";
    }

    /* ── Position dropdown below its input ── */
    function positionDropdown(ul, inputEl) {
      const rect = inputEl.getBoundingClientRect();
      ul.style.top = (rect.bottom + 4) + "px";
      ul.style.left = rect.left + "px";
      ul.style.width = rect.width + "px";
    }

    function closeAllDD() {
      districtDD.style.display = "none";
      talukaDD.style.display = "none";
      districtArrow.classList.remove("open");
      talukaArrow.classList.remove("open");
    }

    /* ── District open/filter ── */
    function openDistrictDD() {
      talukaDD.style.display = "none";
      talukaArrow.classList.remove("open");

      const val = distSearchMode ? districtInput.value.trim().toLowerCase() : "";
      const matches = allDistricts
        .filter(d => !val || d.toLowerCase().includes(val))
        .map(d => ({
          label: d,
          query: val
        }));

      positionDropdown(districtDD, districtInput);
      renderDropdown(districtDD, matches, (name) => {
        selectedDistrict = name;
        districtInput.value = name;
        districtInput.readOnly = true;
        distSearchMode = false;
        districtDD.style.display = "none";
        districtArrow.classList.remove("open");
        districtClear.classList.add("show");
        /* reset taluka when district changes */
        selectedTaluka = "";
        talukaInput.value = "";
        talukaInput.readOnly = true;
        talSearchMode = false;
        talukaClear.classList.remove("show");
      });
      districtArrow.classList.add("open");
    }

    districtInput.addEventListener("click", () => {
      if (!distSearchMode) {
        districtInput.readOnly = false;
        districtInput.value = "";
        distSearchMode = true;
      }
      openDistrictDD();
    });

    districtInput.addEventListener("input", openDistrictDD);

    districtInput.addEventListener("blur", () => {
      setTimeout(() => {
        districtDD.style.display = "none";
        districtArrow.classList.remove("open");
        if (distSearchMode) {
          districtInput.value = selectedDistrict;
          districtInput.readOnly = true;
          distSearchMode = false;
        }
      }, 160);
    });

    /* ── Taluka open/filter ── */
    function openTalukaDD() {
      districtDD.style.display = "none";
      districtArrow.classList.remove("open");

      const val = talSearchMode ? talukaInput.value.trim().toLowerCase() : "";
      let matches;

      if (selectedDistrict) {
        const list = DISTRICT_TALUKA[selectedDistrict] || [];
        matches = list
          .filter(t => !val || t.toLowerCase().includes(val))
          .map(t => ({
            label: t,
            query: val
          }));
      } else {
        matches = allTalukas
          .filter(t => !val || t.toLowerCase().includes(val))
          .map(t => {
            const districts = TALUKA_DISTRICT[t.toLowerCase()] || [];
            return {
              label: t,
              sub: districts.join(" / "),
              query: val
            };
          });
      }

      positionDropdown(talukaDD, talukaInput);
      renderDropdown(talukaDD, matches, (talukaName, districtName) => {
        selectedTaluka = talukaName;
        talukaInput.value = talukaName;
        talukaInput.readOnly = true;
        talSearchMode = false;
        talukaDD.style.display = "none";
        talukaArrow.classList.remove("open");
        talukaClear.classList.add("show");

        /* auto-fill district from taluka if not already set */
        if (!selectedDistrict && districtName) {
          const districts = TALUKA_DISTRICT[talukaName.toLowerCase()] || [];
          if (districts.length) {
            selectedDistrict = districts[0];
            districtInput.value = districts[0];
            districtClear.classList.add("show");
          }
        }
      });
      talukaArrow.classList.add("open");
    }

    talukaInput.addEventListener("click", () => {
      if (!talSearchMode) {
        talukaInput.readOnly = false;
        talukaInput.value = "";
        talSearchMode = true;
      }
      openTalukaDD();
    });

    talukaInput.addEventListener("input", openTalukaDD);

    talukaInput.addEventListener("blur", () => {
      setTimeout(() => {
        talukaDD.style.display = "none";
        talukaArrow.classList.remove("open");
        if (talSearchMode) {
          talukaInput.value = selectedTaluka;
          talukaInput.readOnly = true;
          talSearchMode = false;
        }
      }, 160);
    });

    /* ── Reposition on scroll/resize ── */
    window.addEventListener("scroll", () => {
      if (districtDD.style.display === "block") positionDropdown(districtDD, districtInput);
      if (talukaDD.style.display === "block") positionDropdown(talukaDD, talukaInput);
    }, true);
    window.addEventListener("resize", closeAllDD);

    /* ── Clear buttons ── */
    function clearDistrict() {
      selectedDistrict = "";
      districtInput.value = "";
      districtInput.readOnly = true;
      distSearchMode = false;
      districtClear.classList.remove("show");
      districtDD.style.display = "none";
      districtArrow.classList.remove("open");
      selectedTaluka = "";
      talukaInput.value = "";
      talukaInput.readOnly = true;
      talSearchMode = false;
      talukaClear.classList.remove("show");
    }

    function clearTaluka() {
      selectedTaluka = "";
      talukaInput.value = "";
      talukaInput.readOnly = true;
      talSearchMode = false;
      talukaClear.classList.remove("show");
      talukaDD.style.display = "none";
      talukaArrow.classList.remove("open");
    }

    /* ── Role change ── */
    function handleRoleChange(val) {
      clearDistrict();
    }

    /* ── Modal ── */
    function openForm() {
      var m = document.getElementById("inquiryModal");
      m.style.display = "flex";
      setTimeout(function() {
        m.classList.add("open");
      }, 10);
    }

    function closeForm() {
      document.getElementById("inquiryModal").style.display = "none";
      document.getElementById("inquiryModal").classList.remove("open");
      closeAllDD();
    }

    window.onclick = function(e) {
      var modal = document.getElementById("inquiryModal");
      if (e.target === modal) closeForm();
    };

    /* close dropdowns when clicking outside combo areas */
    document.addEventListener("mousedown", e => {
      if (!document.getElementById("districtCombo").contains(e.target) &&
        !districtDD.contains(e.target)) {
        districtDD.style.display = "none";
        districtArrow.classList.remove("open");
        if (distSearchMode) {
          districtInput.value = selectedDistrict;
          districtInput.readOnly = true;
          distSearchMode = false;
        }
      }
      if (!document.getElementById("talukaCombo").contains(e.target) &&
        !talukaDD.contains(e.target)) {
        talukaDD.style.display = "none";
        talukaArrow.classList.remove("open");
        if (talSearchMode) {
          talukaInput.value = selectedTaluka;
          talukaInput.readOnly = true;
          talSearchMode = false;
        }
      }
    });
  </script>

  <style>
    /* ─── Brand Variables ─── */
    :root {
      --brand: #e8834a;
      --brand-dark: #c9642e;
      --brand-deep: #7a3515;
      --brand-light: #fdf2eb;
      --brand-glow: rgba(232, 131, 74, 0.25);
    }

    /* ─── Highlight match ─── */
    .ac-dropdown mark {
      background: rgba(232, 131, 74, 0.25);
      color: var(--brand-dark);
      border-radius: 2px;
      padding: 0 1px;
    }

    /* ─── Inquiry Button ─── */
    .inquiry-btn {
      position: fixed;
      background: linear-gradient(180deg, var(--brand) 0%, var(--brand-dark) 100%);
      color: #fff;
      text-decoration: none;
      font-weight: 600;
      font-size: 13px;
      letter-spacing: 0.5px;
      z-index: 1000;
      display: flex;
      align-items: center;
      gap: 6px;
      box-shadow: 0 4px 16px var(--brand-glow), 0 2px 6px rgba(0, 0, 0, 0.15);
      transition: background 0.2s, box-shadow 0.2s;
    }

    .inquiry-btn:hover {
      background: linear-gradient(180deg, #f0934e 0%, #d9723b 100%);
      box-shadow: 0 6px 20px var(--brand-glow);
    }

    @media (min-width: 768px) {
      .inquiry-btn {
        right: 25px !important;
        top: 50vh !important;
        transform: translateY(-50%) rotate(-90deg) !important;
        transform-origin: right center !important;
        padding: 11px 22px;
        border-radius: 8px 8px 0 0;
        white-space: nowrap;
        position: fixed !important;
      }

      .inq-icon {
        font-size: 18px;
      }
    }

    @media (max-width: 767px) {
      .inquiry-btn {
        bottom: 0;
        left: 0;
        width: 100%;
        justify-content: center;
        border-radius: 0;
        padding: 15px 18px;
        font-size: 15px;
      }
    }

    /* ─── Modal Overlay ─── */
    .modal {
      display: none;
      position: fixed;
      z-index: 20000;
      inset: 0;
      background: rgba(20, 10, 5, 0.65);
      backdrop-filter: blur(3px);
      -webkit-backdrop-filter: blur(3px);
      align-items: center;
      justify-content: center;
    }

    .modal.open {
      display: flex;
    }

    /* ─── Modal Box ─── */
    .modal-content {
      background: #fff;
      width: 95%;
      max-width: 700px;
      max-height: 90vh;
      border-radius: 16px;
      overflow: hidden;
      animation: slideUp 0.3s cubic-bezier(0.34, 1.3, 0.64, 1);
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.25), 0 0 0 1px rgba(0, 0, 0, 0.06);
      display: flex;
      flex-direction: column;
    }

    .modal-body {
      overflow-y: auto;
      padding: 22px 20px 20px;
      background: #fff;
      flex: 1;
    }

    .modal-body form {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 14px;
    }

    .form-group.full {
      grid-column: 1 / -1;
    }

    /* ─── Modal Header ─── */
    .modal-header {
      background: linear-gradient(135deg, var(--brand) 0%, var(--brand-dark) 100%);
      padding: 18px 20px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-shrink: 0;
    }

    .modal-header-left {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .modal-icon-wrap {
      color: white;
      width: 40px;
      height: 40px;
      background: rgba(255, 255, 255, 0.2);
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 18px;
    }

    .modal-header h3 {
      margin: 0;
      font-size: 17px;
      font-weight: 700;
      color: #fff;
      line-height: 1.2;
    }

    .modal-sub {
      margin: 2px 0 0;
      font-size: 12px;
      color: rgba(255, 255, 255, 0.78);
    }

    .close {
      width: 30px;
      height: 30px;
      background: rgba(255, 255, 255, 0.2);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      font-size: 14px;
      color: #fff;
      transition: background 0.2s;
      flex-shrink: 0;
      line-height: 1;
    }

    .close:hover {
      background: rgba(255, 255, 255, 0.35);
    }

    /* ─── Form Groups ─── */
    .form-group {
      margin-bottom: 0;
      position: relative;
    }

    .form-group label {
      display: block;
      font-size: 12px;
      font-weight: 600;
      color: var(--brand-deep);
      text-transform: uppercase;
      letter-spacing: 0.6px;
      margin-bottom: 6px;
    }

    /* ─── Input Wrap ─── */
    .input-wrap {
      position: relative;
      display: flex;
      align-items: center;
    }

    .input-icon {
      position: absolute;
      left: 11px;
      font-size: 14px;
      pointer-events: none;
      z-index: 1;
    }

    .input-wrap input,
    .input-wrap select {
      width: 100%;
      padding: 10px 12px 10px 36px;
      border: 1.5px solid #e8d5c8;
      border-radius: 9px;
      font-size: 14px;
      color: #333;
      background: var(--brand-light);
      box-sizing: border-box;
      outline: none;
      transition: border-color 0.2s, background 0.2s, box-shadow 0.2s;
      appearance: none;
      -webkit-appearance: none;
    }

    .input-wrap input:focus,
    .input-wrap select:focus {
      border-color: var(--brand);
      background: #fff;
      box-shadow: 0 0 0 3px var(--brand-glow);
    }

    .input-wrap input::placeholder {
      color: #bba898;
    }

    /* ─── Combo extras ─── */
    .combo-wrap {
      position: relative;
    }

    .combo-wrap input {
      cursor: pointer;
      padding-right: 52px;
    }

    .combo-wrap input:not([readonly]) {
      cursor: text;
    }

    .combo-arrow {
      position: absolute;
      right: 10px;
      top: 50%;
      transform: translateY(-50%);
      font-size: 10px;
      color: #bba898;
      pointer-events: none;
      transition: transform 0.2s;
    }

    .combo-arrow.open {
      transform: translateY(-50%) rotate(180deg);
    }

    .combo-clear {
      position: absolute;
      right: 26px;
      top: 50%;
      transform: translateY(-50%);
      font-size: 12px;
      color: #bba898;
      cursor: pointer;
      display: none;
      line-height: 1;
      z-index: 2;
      transition: color 0.15s;
    }

    .combo-clear:hover {
      color: var(--brand-dark);
    }

    .combo-clear.show {
      display: block;
    }

    /* ─── Autocomplete Dropdown ─── */
    .ac-dropdown {
      display: none;
      position: fixed;
      background: #fff;
      border: 1.5px solid #e8d5c8;
      border-radius: 10px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
      max-height: 200px;
      overflow-y: auto;
      z-index: 99999;
      list-style: none;
      margin: 0;
      padding: 4px 0;
    }

    .ac-dropdown li {
      padding: 9px 14px;
      font-size: 13px;
      color: #333;
      cursor: pointer;
      transition: background 0.12s;
      display: flex;
      flex-direction: column;
      gap: 2px;
    }

    .ac-dropdown li:hover {
      background: var(--brand-light);
      color: var(--brand-dark);
    }

    .ac-no-result {
      padding: 9px 14px;
      font-size: 13px;
      color: #bba898;
      pointer-events: none;
    }

    .ac-main {
      font-size: 13px;
      font-weight: 500;
    }

    .ac-sub {
      font-size: 11px;
      color: #a07050;
    }

    /* ─── Submit Button ─── */
    .submit-btn {
      width: 100%;
      margin-top: 4px;
      padding: 13px;
      background: linear-gradient(135deg, var(--brand) 0%, var(--brand-dark) 100%);
      color: #fff;
      border: none;
      border-radius: 10px;
      font-size: 15px;
      font-weight: 700;
      letter-spacing: 0.4px;
      cursor: pointer;
      transition: opacity 0.2s, transform 0.15s, box-shadow 0.2s;
      box-shadow: 0 4px 14px var(--brand-glow);
    }

    .submit-btn:hover {
      opacity: 0.93;
      transform: translateY(-1px);
      box-shadow: 0 6px 18px var(--brand-glow);
    }

    .submit-btn:active {
      transform: scale(0.98);
    }

    /* ─── Animation ─── */
    @keyframes slideUp {
      from {
        opacity: 0;
        transform: translateY(20px) scale(0.97);
      }

      to {
        opacity: 1;
        transform: translateY(0) scale(1);
      }
    }

    /* ─── Mobile ─── */
    @media (max-width: 767px) {
      .modal-content {
        width: 96%;
        max-width: 100%;
        border-radius: 12px;
      }

      .modal-body {
        padding: 18px 14px 25px;
      }

      .modal-body form {
        grid-template-columns: 1fr;
        gap: 12px;
      }

      .input-wrap input,
      .input-wrap select {
        padding: 12px 12px 12px 38px;
        font-size: 15px;
      }

      .combo-wrap input {
        padding-right: 52px;
      }

      .submit-btn {
        padding: 14px;
        font-size: 16px;
      }

      .modal-header {
        padding: 14px;
      }

      .modal-header h3 {
        font-size: 15px;
      }

      .modal-sub {
        font-size: 11px;
      }

      .modal-icon-wrap {
        width: 34px;
        height: 34px;
        font-size: 16px;
      }
    }
  </style>

  <!--
═══════════════════════════════════════════════════════════════════
  GOOGLE APPS SCRIPT — આ code તમારા Apps Script માં ઉમેરો
  (Tools → Script Editor in Google Sheets)
  
  આ code ?action=getAgents request handle કરે છે અને
  Sheet1 માં save થયેલ agents નો JSON return કરે છે.
═══════════════════════════════════════════════════════════════════

function doPost(e) {
  return handleRequest(e);
}

function doGet(e) {
  if (e.parameter.action === "getAgents") {
    return getAgents();
  }
  return ContentService.createTextOutput("OK");
}

function getAgents() {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName("Sheet1");
  var data  = sheet.getDataRange().getValues();
  var headers = data[0].map(h => h.toString().toLowerCase().trim());
  
  var agents = [];
  for (var i = 1; i < data.length; i++) {
    var row = {};
    headers.forEach(function(h, idx) { row[h] = data[i][idx]; });
    if (row["role"] && row["role"].toString().toLowerCase() === "agent") {
      agents.push({
        name:     row["name"]     || "",
        number:   row["number"]   || "",
        address:  row["address"]  || "",
        district: row["district"] || "",
        taluka:   row["taluka"]   || "",
        role:     "agent"
      });
    }
  }
  
  return ContentService
    .createTextOutput(JSON.stringify(agents))
    .setMimeType(ContentService.MimeType.JSON);
}

function handleRequest(e) {
  try {
    var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName("Sheet1");
    var headers = sheet.getRange(1, 1, 1, sheet.getLastColumn()).getValues()[0];
    var nextRow = sheet.getLastRow() + 1;
    
    var row = headers.map(function(h) {
      return e.parameter[h] || "";
    });
    sheet.getRange(nextRow, 1, 1, row.length).setValues([row]);
    
    return ContentService
      .createTextOutput(JSON.stringify({ result: "success" }))
      .setMimeType(ContentService.MimeType.JSON);
  } catch(err) {
    return ContentService
      .createTextOutput(JSON.stringify({ result: "error", error: err.toString() }))
      .setMimeType(ContentService.MimeType.JSON);
  }
}

═══════════════════════════════════════════════════════════════════
  Google Sheet ના Column headers આ હોવા જોઈએ (Row 1):
  role | district | taluka | name | number | address | ...
═══════════════════════════════════════════════════════════════════
-->

  <script data-cfasync="false" type="text/javascript">
    (function() {
      function validEmail(email) {
        var re = /^([\w-]+(?:\.[\w-]+)*)@((?:[\w-]+\.)*\w[\w-]{0,66})\.([a-z]{2,6}(?:\.[a-z]{2})?)$/i;
        return re.test(email);
      }

      function validateHuman(honeypot) {
        if (honeypot) {
          console.log("Robot Detected!");
          return true;
        } else {
          console.log("Welcome Human!");
        }
      }

      function getFormData(form) {
        var elements = form.elements;

        var fields = Object.keys(elements).filter(function(k) {
          return (elements[k].name !== "honeypot");
        }).map(function(k) {
          if (elements[k].name !== undefined) {
            return elements[k].name;
          } else if (elements[k].length > 0) {
            return elements[k].item(0).name;
          }
        }).filter(function(item, pos, self) {
          return self.indexOf(item) == pos && item;
        });

        var formData = {};
        fields.forEach(function(name) {
          var element = elements[name];
          formData[name] = element.value;
          if (element.length) {
            var data = [];
            for (var i = 0; i < element.length; i++) {
              var item = element.item(i);
              if (item.checked || item.selected) {
                data.push(item.value);
              }
            }
            formData[name] = data.join(', ');
          }
        });

        // add form-specific values into the data
        formData.formDataNameOrder = JSON.stringify(fields);
        formData.formGoogleSheetName = form.dataset.sheet || "Sheet1"; // default sheet name
        formData.formGoogleSendEmail = form.dataset.email || ""; // no email by default

        console.log(formData);
        return formData;
      }

      function handleFormSubmit(event) {
        event.preventDefault();
        var form = event.target;
        var data = getFormData(form);
        if (data.email && !validEmail(data.email)) {
          var invalidEmail = form.querySelector(".email-invalid");
          if (invalidEmail) {
            invalidEmail.style.display = "block";
            return false;
          }
        } else {
          disableAllButtons(form);
          var url = form.action;
          var xhr = new XMLHttpRequest();
          xhr.open('POST', url);
          xhr.setRequestHeader("Content-Type", "application/x-www-form-urlencoded");
          xhr.onreadystatechange = function() {
            console.log(xhr.status, xhr.statusText);
            console.log(xhr.responseText);
            var formElements = form.querySelector(".form-elements")
            if (formElements) {
              formElements.style.display = "none"; // hide form
            }
            var thankYouMessage = form.querySelector(".thankyou_message");
            if (thankYouMessage) {
              thankYouMessage.style.display = "block";
            }
            return;
          };
          var encoded = Object.keys(data).map(function(k) {
            return encodeURIComponent(k) + "=" + encodeURIComponent(data[k]);
          }).join('&');
          xhr.send(encoded);
        }
      }

      function loaded() {
        console.log("Contact form submission handler loaded successfully.");
        var forms = document.querySelectorAll("form.gform");
        for (var i = 0; i < forms.length; i++) {
          forms[i].addEventListener("submit", handleFormSubmit, false);
        }
      };
      document.addEventListener("DOMContentLoaded", loaded, false);

      function disableAllButtons(form) {
        var buttons = form.querySelectorAll("button");
        for (var i = 0; i < buttons.length; i++) {
          buttons[i].disabled = true;
        }
      }
    })();
  </script>

</body>

</html>